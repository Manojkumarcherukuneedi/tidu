[pytest]
testpaths = backend/tests
addopts = -q
# Library-internal deprecation noise on newer Python; not from our code.
filterwarnings =
    ignore:'asyncio.iscoroutinefunction' is deprecated:DeprecationWarning
